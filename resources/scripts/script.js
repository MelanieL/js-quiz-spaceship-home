const shipHome = {};

shipHome.answers = {
    nostromo: 0,
    milano: 0,
    millfalc: 0, 
    enterprise: 0,
    bsg: 0,
    serenity: 0,
    eaglefive: 0,
    rocinante: 0
};

// Need to allow user to only select one answer to each question
// On submit, need to pull all selections
// Need to evaluate all selections 
// Need to total points for each ship
// Need to determine which ship is their answer
// Need to display ship/answer